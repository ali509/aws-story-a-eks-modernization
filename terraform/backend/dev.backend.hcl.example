bucket         = "botconsulting-story-a-dev-tfstate"
key            = "dev/platform/terraform.tfstate"
region         = "ap-south-1"
encrypt        = true
dynamodb_table = "botconsulting-story-a-dev-tf-lock"

# Account IDs, bucket names, region and locking choice require client input.
